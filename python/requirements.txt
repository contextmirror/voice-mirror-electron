# Voice Mirror dependencies

# Wake word detection
openwakeword>=0.6.0,<1.0.0

# Audio capture
sounddevice>=0.4.6,<0.6.0
numpy>=2.0.2,<3.0.0

# Claude API for CLI responses (optional)
anthropic>=0.40.0,<1.0.0

# Local STT (Parakeet) — gpu for CUDA acceleration, hub for HuggingFace model downloads
onnx-asr[gpu,hub]>=0.10.0,<1.0.0

# Local TTS (Kokoro)
kokoro-onnx>=0.4.0,<1.0.0
soundfile>=0.13.0,<0.14.0
psutil>=5.9.0,<6.0.0

# Global hotkey (PTT) on Windows/macOS/X11
pynput>=1.7.0,<2.0.0
