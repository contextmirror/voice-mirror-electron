# electron/window/

Window management for the Electron overlay.

## Module index

| Module | Export | Description |
|---|---|---|
| `index.js` | `createWindowManager()`, `clampToVisibleArea()`, `createTrayService()` | Barrel + window manager factory + multi-monitor clamping |
| `tray.js` | `createTrayService()` | System tray icon with context menu |
